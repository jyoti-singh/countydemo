<%= form_for(@countytype) do |f| %>
  <% if @countytype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@countytype.errors.count, "error") %> prohibited this countytype from being saved:</h2>

      <ul>
      <% @countytype.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :type %><br>
    <%= f.text_field :type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
